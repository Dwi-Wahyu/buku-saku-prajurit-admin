import UnderConstructionPage from "@/app/_components/under-construction";

export default function RiwayatPenilaianKesegaranJasmaniPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  return <UnderConstructionPage />;
}
